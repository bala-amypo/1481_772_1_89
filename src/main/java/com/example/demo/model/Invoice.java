 ✅ Next step

Invoice model la getDescription() method add pannunga:

public String getDescription() {
    // return some meaningful description for categorization
    return "Invoice from vendor " + vendorId + " amount " + amount;
}


InvoiceServiceImpl la engine.applyRules(...) → engine.categorize(...) use pannunga.

Clean build: